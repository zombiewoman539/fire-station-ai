---
name: Run commands directly
description: Always run terminal commands directly rather than asking the user to do it
type: feedback
originSessionId: 306780e8-302b-47fe-a1cc-6e2b824c3bba
---
Always run commands directly using the Bash tool rather than asking the user to copy-paste and run them manually.

**Why:** User prefers I handle all terminal operations myself.

**How to apply:** If a command needs user interaction (e.g. browser login), explain why I can't run it and what the user specifically needs to do — but for everything else, just run it.
